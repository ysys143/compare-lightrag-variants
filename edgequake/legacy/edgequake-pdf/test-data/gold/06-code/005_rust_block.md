# Rust Code Block

```rust
fn main() {
    let message = "Hello, Rust!";
    println!("{}", message);

    let numbers = vec![1, 2, 3, 4, 5];
    for num in numbers {
        println!("{}", num);
    }
}
```

Rust code example above.
