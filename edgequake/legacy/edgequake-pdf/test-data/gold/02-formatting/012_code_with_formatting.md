# Code with Formatting

Text with **bold** and `code` mixed.

_Italic_ and `code` testing combinations.

**Bold `code`** inside.

_Italic `code`_ inside.

`code with **bold** inside` if supported.

Testing `code` with **bold** `more code` _italic_ end.
