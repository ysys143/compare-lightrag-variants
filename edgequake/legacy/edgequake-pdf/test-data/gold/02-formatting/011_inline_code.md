# Inline Code 1

This is `inline code` in text.

Testing `variable_name` and `function()` inline.

Code: `const x = 42;` is valid.

Multiple `code` and `more code` and `final code` in one line.

`Long inline code with spaces and symbols like @#$%` testing extended inline.
